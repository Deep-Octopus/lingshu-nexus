# Configuration

This directory is reserved for versioned, non-secret project configuration.

T-000 only creates the directory and environment template. Domain-specific
schemas and vocabularies start in T-010 and must remain versioned and free of
real API keys, passwords, or access tokens.

